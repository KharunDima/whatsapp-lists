# Domain Scanner Pro v5.0

Инструмент для поиска доменов и сетей, используемых сервисами.

## Возможности

- 🔍 **Множественные источники данных**:
    - CRT.SH сертификаты
    - DNS brute force
    - Пассивные источники
    - Статические конфигурации
- 🌐 **Асинхронный DNS резолвер** с балансировкой и кэшированием
- 🛡️ **Валидация и фильтрация** доменов
- 📊 **Автоматическая генерация CIDR сетей** из найденных IP
- 💾 **Гибкое сохранение результатов** в различных форматах
- ⚡ **Высокая производительность** благодаря асинхронной архитектуре

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ваш-username/whatsapp-discovery.git
cd whatsapp-discovery
```
Установите зависимости:

```bash
pip install -r requirements.txt
```
Настройте конфигурацию в папке config/targets/

Использование
Базовые команды
```bash
# Показать список доступных целей
python main.py --list-targets

# Создать новую цель
python main.py --create-target <имя>

# Запустить поиск для WhatsApp
python main.py whatsapp

# Запустить с дополнительными параметрами
python main.py whatsapp --timeout 60 --concurrent 200 --log-level DEBUG
```
Параметры командной строки
target - Цель поиска (например: whatsapp)

--list-targets - Показать список доступных целей

--create-target NAME - Создать шаблон конфигурации

--timeout SECONDS - Таймаут запросов (по умолчанию: 30)

--concurrent N - Количество одновременных запросов (по умолчанию: 200)

--output-dir DIR - Директория для результатов (по умолчанию: results)

--log-level LEVEL - Уровень логирования (DEBUG, INFO, WARNING, ERROR)

--log-file FILE - Файл для сохранения логов

Конфигурация
Конфигурация целей хранится в config/targets/ в формате JSON. Пример для WhatsApp:

json
{
  "name": "whatsapp",
  "description": "WhatsApp domains and networks",
  "keywords": ["whatsapp", "wa"],
  "static_domains": ["whatsapp.com", "web.whatsapp.com"],
  "static_cidrs": ["31.13.0.0/16"],
  "crt_sh_patterns": ["%.whatsapp.net", "%.whatsapp.com"],
  "domain_patterns": [".+\\.whatsapp\\.(com|net)$"]
}
Структура проекта
text
whatsapp-discovery/
├── config/              # Конфигурация приложения
│   ├── settings.py     # Основные настройки
│   └── targets/        # Конфигурации целей
├── core/               # Основные компоненты
│   ├── analyzer.py     # Анализ IP и создание CIDR
│   ├── resolver.py     # DNS резолвер
│   └── saver.py        # Сохранение результатов
├── sources/            # Источники данных
│   ├── base.py         # Базовый класс источников
│   ├── crtsh.py        # CRT.SH источник
│   ├── dns_bruteforce.py # DNS brute force
│   ├── passive.py      # Пассивные источники
│   └── static.py       # Статические данные
├── utils/              # Вспомогательные функции
│   ├── helpers.py      # Утилиты
│   └── validator.py    # Валидатор доменов
├── main.py             # Главный скрипт
├── requirements.txt    # Зависимости
└── README.md          # Документация
Результаты
После выполнения сканирования в папке results/ создаются следующие файлы:

domains.txt - Список найденных доменов

cidr_ipv4.txt - IPv4 CIDR сети

cidr_ipv6.txt - IPv6 CIDR сети

report.json - Полный отчет в формате JSON

Зависимости
См. файл requirements.txt для списка зависимостей.

Лицензия
MIT License

Вклад в проект
Форкните репозиторий

Создайте ветку для вашей функции

Зафиксируйте изменения

Отправьте pull request

Поддержка
Для вопросов и предложений создавайте issue в репозитории.


